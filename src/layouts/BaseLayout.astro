---
import '../styles/global.css';
import SiteHeader from '../components/SiteHeader.astro';
import Footer from '../components/Footer.astro';
import SEOHead from '../components/SEOHead.astro';
import Fonts from '../components/Fonts.astro';
import { getLangFromUrl, t } from '../i18n/utils';

interface Props {
  title?: string;
  description?: string;
  image?: string;
  type?: 'website' | 'article';
  publishedTime?: string;
  modifiedTime?: string;
  jsonLd?: Record<string, any> | Record<string, any>[];
}

const lang = getLangFromUrl(Astro.url);
const { title, description, image, type, publishedTime, modifiedTime, jsonLd } = Astro.props;

const pageTitle = title
  ? `${title} | ${t(lang, 'site.title')}`
  : t(lang, 'site.title');
const pageDescription = description ?? t(lang, 'site.description');
const currentPath = Astro.url.pathname;
---

<!doctype html>
<html lang={lang}>
  <head>
    <SEOHead
      title={pageTitle}
      description={pageDescription}
      lang={lang}
      currentPath={currentPath}
      image={image}
      type={type}
      publishedTime={publishedTime}
      modifiedTime={modifiedTime}
      jsonLd={jsonLd}
    />
    <Fonts />
  </head>
  <body class="min-h-screen flex flex-col bg-sand text-ink antialiased">
    <SiteHeader lang={lang} currentPath={currentPath} solid />
    <main class="flex-1">
      <slot />
    </main>
    <Footer lang={lang} />
  </body>
</html>
