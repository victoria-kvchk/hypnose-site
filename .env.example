# Copier ce fichier en `.env` puis renseigner les valeurs.
# `.env` est ignoré par git (ne jamais committer de secrets).

# Newsletter — URL publique du formulaire d'inscription Brevo.
# Brevo → Contacts → Formulaires → ton formulaire → onglet « Partager ».
# Format : https://<sous-domaine>.sibforms.com/serve/XXXX...
PUBLIC_BREVO_FORM_ACTION=""
